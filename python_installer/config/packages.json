{
  "packages": [
    {
      "name": "curl",
      "origin": "default",
      "profiles": ["default", "home", "work"]
    },
    {
      "name": "git",
      "origin": "default",
      "profiles": ["default", "home"]
    },
    {
      "name": "nodejs",
      "origin": {
        "ubuntu": "default",
        "arch": "sudo pacman -S nodejs",
        "fallback": "curl -fsSL https://deb.nodesource.com/setup_14.x | sudo -E bash - && sudo apt-get install -y nodejs"
      },
      "profiles": ["home"]
    }
  ]
}

